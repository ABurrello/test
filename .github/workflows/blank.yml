name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
      
  tests:

    runs-on: ubuntu-bionic
    container:
        image: ubuntu:bionic
    steps:
      - name: Run experiments
        run: |
          source pippo.sh
